//From header to footer
import {
  renderHeader,
  renderNavbar,
  renderFooter,
  setForwaringAddress,
} from "../modules/methods.js";

renderHeader();
renderNavbar();
renderFooter();

setForwaringAddress();
